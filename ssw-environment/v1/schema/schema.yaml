schema:
  format:
    openapi: "3.0.0"
  environment_input_type: "EnvironmentInput"
  types:
    EnvironmentInput:
      type: object
      description: "S3 static website environment inputs"
      required:
        - project_name
      properties:
        project_name:
          type: string
          description: "A name for your Project"
